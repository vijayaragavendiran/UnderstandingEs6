'use strict'
// spread

function spread(){
  let firstArray = ['c','d','e'];
  let secondArray=['g','h'];
  // In this example spread allows to expand the array by ...
  console.log('spread results : ' + 'a','b',...firstArray,'f',...secondArray);
}

spread();

//
